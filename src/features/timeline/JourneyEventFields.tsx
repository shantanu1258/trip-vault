import { ChevronDown, Plus, Trash2 } from "lucide-react";
import { useEffect, useState, type ReactNode } from "react";
import { TimeZoneAutocomplete } from "../../components/TimeZoneAutocomplete";
import { RequiredMark } from "../../components/RequiredMark";
import { AirlinePicker } from "../metadata/AirlinePicker";
import { AirportPicker } from "../metadata/AirportPicker";
import type { AvailableAirport } from "../metadata/publishedConfig";
import { JourneyOperatorPicker } from "../metadata/JourneyOperatorPicker";
import type { Trip } from "../trips/types";
import { isoToLocalDateTime, localDateTimeMinusMinutes, localDateTimeToIso } from "../trips/validation";
import type { FlightLeg, JourneyMode, JourneyScope, ReservationState, Traveler } from "../workspace/types";

export type RouteStructure = "direct" | "connecting";
export type FlightLegDraft = { arrivalAirport: AvailableAirport | null; arrivalLocal: string };

export function suggestedFlightLocalTime(local: string, fromTimeZone: string, toTimeZone: string, minutesLater: number) {
  if (!local || !fromTimeZone || !toTimeZone) return "";
  try {
    const instant = localDateTimeToIso(local, fromTimeZone, "earlier");
    return isoToLocalDateTime(new Date(Date.parse(instant) + minutesLater * 60_000).toISOString(), toTimeZone);
  } catch {
    return "";
  }
}

function OccurrenceSelect({ name }: { name: string }) {
  return <select className="form-input" name={name} defaultValue="automatic" aria-label="Repeated clock time choice"><option value="automatic">Automatic (usual)</option><option value="earlier">Earlier occurrence</option><option value="later">Later occurrence</option></select>;
}

function RepeatedClockHelp() {
  return <p className="mt-2 text-xs leading-5 text-muted">Use Automatic normally. Choose an occurrence only if daylight saving makes the printed local time happen twice.</p>;
}

function endpointLabel(code: string | null | undefined, name: string | null | undefined) {
  return code?.trim().toUpperCase() || name?.trim() || "";
}

function JourneyLegCard({ title, subtitle, removeLabel, onRemove, children }: { title: string; subtitle?: string; removeLabel?: string; onRemove?: () => void; children: ReactNode }) {
  const [open, setOpen] = useState(true);
  return <details open={open} onToggle={(event) => setOpen(event.currentTarget.open)} className="group rounded-2xl border border-line p-4">
    <summary className="flex cursor-pointer list-none items-center justify-between gap-3 [&::-webkit-details-marker]:hidden">
      <span className="min-w-0"><strong className="block font-display text-lg font-black capitalize">{title}</strong>{subtitle && <span className="mt-0.5 block truncate text-xs text-muted">{subtitle}</span>}</span>
      <span className="flex shrink-0 items-center gap-1">{onRemove && <button type="button" className="tap-target grid size-9 place-items-center text-danger" onClick={(event) => { event.preventDefault(); event.stopPropagation(); onRemove(); }} aria-label={removeLabel}><Trash2 className="size-4" /></button>}<ChevronDown className="size-5 text-muted transition-transform group-open:rotate-180" aria-hidden="true" /></span>
    </summary>
    <div className="pt-4">{children}</div>
  </details>;
}

export function JourneyScopeFields({ value, onChange }: { value: JourneyScope; onChange: (value: JourneyScope) => void }) {
  return <fieldset className="grid grid-cols-2 gap-3 rounded-2xl border border-line p-4"><legend className="px-1 text-sm font-extrabold">Journey type</legend><label className={`flex items-center gap-2 rounded-xl border p-3 text-sm font-bold ${value === "domestic" ? "border-brand bg-brand-soft" : "border-line bg-elevated"}`}><input type="radio" name="journeyScope" value="domestic" checked={value === "domestic"} onChange={() => onChange("domestic")} /> Domestic</label><label className={`flex items-center gap-2 rounded-xl border p-3 text-sm font-bold ${value === "international" ? "border-brand bg-brand-soft" : "border-line bg-elevated"}`}><input type="radio" name="journeyScope" value="international" checked={value === "international"} onChange={() => onChange("international")} /> International</label></fieldset>;
}

export function RouteStructureFields({ mode, value, onChange }: { mode: "flight" | Exclude<JourneyMode, "cab">; value: RouteStructure; onChange: (value: RouteStructure) => void }) {
  const noun = mode === "flight" ? "flight" : mode === "ferry" ? "sailing" : mode;
  return <fieldset className="grid grid-cols-2 gap-3 rounded-2xl border border-line p-4"><legend className="px-1 text-sm font-extrabold">Route</legend><label className={`flex items-center gap-2 rounded-xl border p-3 text-sm font-bold ${value === "direct" ? "border-brand bg-brand-soft" : "border-line bg-elevated"}`}><input type="radio" name="journeyStructure" value="direct" checked={value === "direct"} onChange={() => onChange("direct")} /> {mode === "flight" ? "Direct" : `Single ${noun}`}</label><label className={`flex items-center gap-2 rounded-xl border p-3 text-sm font-bold ${value === "connecting" ? "border-brand bg-brand-soft" : "border-line bg-elevated"}`}><input type="radio" name="journeyStructure" value="connecting" checked={value === "connecting"} onChange={() => onChange("connecting")} /> Connecting {mode === "flight" ? "flights" : `${noun}s`}</label></fieldset>;
}

function TravelerAllocationFields({ prefix, travelers, mode, showSeatAndCabin = true }: { prefix: string; travelers: Traveler[]; mode: "flight" | JourneyMode; showSeatAndCabin?: boolean }) {
  if (!travelers.length) return null;
  const seatLabel = mode === "train" ? "Berth or seat" : "Seat";
  return <details className="mt-4 rounded-xl border border-line/80 p-3"><summary className="cursor-pointer text-xs font-black uppercase tracking-[.1em] text-muted">Traveler ticket details</summary><p className="mt-2 text-xs leading-5 text-muted">Only fill details already shown on the ticket. They can be updated later.</p><div className="mt-3 space-y-3">{travelers.map((traveler) => <fieldset key={traveler.id} className="rounded-xl bg-elevated p-3"><legend className="px-1 text-sm font-extrabold">{traveler.display_name}</legend><div className="mt-2 grid gap-3 sm:grid-cols-3">{showSeatAndCabin && <label className="form-label text-xs">{seatLabel}<input className="form-input" name={`${prefix}.traveler.${traveler.id}.seat`} placeholder={mode === "ferry" ? "Enter the assigned seat" : "Enter the seat or berth, if assigned"} /></label>}{mode === "flight" && <label className="form-label text-xs">Boarding group<input className="form-input" name={`${prefix}.traveler.${traveler.id}.group`} placeholder="Enter the boarding group, if provided" /></label>}<label className="form-label text-xs">{mode === "flight" ? "Passenger ticket number" : "Passenger reference"}<input className="form-input" name={`${prefix}.traveler.${traveler.id}.reference`} placeholder="Enter the passenger-specific reference, if provided" /></label>{mode === "train" && <label className="form-label text-xs">Coach<input className="form-input" name={`${prefix}.traveler.${traveler.id}.coach`} placeholder="Enter the coach, if provided" /></label>}{mode === "ferry" && showSeatAndCabin && <label className="form-label text-xs">Cabin<input className="form-input" name={`${prefix}.traveler.${traveler.id}.coach`} placeholder="Enter the cabin, if provided" /></label>}</div></fieldset>)}</div></details>;
}

export function FlightLegFields({ index, trip, scope, travelers, direct, removable, previousLeg, onArrivalChange, onRemove }: { index: number; trip: Trip; scope: JourneyScope; travelers: Traveler[]; direct: boolean; removable: boolean; previousLeg?: FlightLegDraft; onArrivalChange?: (value: FlightLegDraft) => void; onRemove: () => void }) {
  const prefix = `flight.${index}`;
  const connectionDeparture = previousLeg?.arrivalAirport ? suggestedFlightLocalTime(previousLeg.arrivalLocal, previousLeg.arrivalAirport.timezone, previousLeg.arrivalAirport.timezone, 120) : "";
  const [departure, setDeparture] = useState(connectionDeparture || `${trip.start_date}T09:00`);
  const [departureEdited, setDepartureEdited] = useState(false);
  const [departureAirport, setDepartureAirport] = useState<AvailableAirport | null>(previousLeg?.arrivalAirport ?? null);
  const [arrivalAirport, setArrivalAirport] = useState<AvailableAirport | null>(null);
  const [arrival, setArrival] = useState(`${trip.start_date}T12:00`);
  const [arrivalEdited, setArrivalEdited] = useState(false);
  const [boardingLead, setBoardingLead] = useState("");
  const [departureCountry, setDepartureCountry] = useState("");
  const international = scope === "international";
  const calculatedBoarding = boardingLead ? localDateTimeMinusMinutes(departure, Number(boardingLead)) : "";
  const departureLabel = endpointLabel(departureAirport?.iataCode ?? departureAirport?.icaoCode, departureAirport?.name);
  const arrivalLabel = endpointLabel(arrivalAirport?.iataCode ?? arrivalAirport?.icaoCode, arrivalAirport?.name);
  const heading = departureLabel && arrivalLabel ? `${departureLabel} → ${arrivalLabel} flight` : direct ? "Flight details" : index ? `Connection ${index + 1}` : "First flight";
  const routeDescription = departureAirport && arrivalAirport ? `${departureAirport.city || departureAirport.name} to ${arrivalAirport.city || arrivalAirport.name}` : undefined;
  useEffect(() => {
    if (!previousLeg) return;
    setDepartureAirport(previousLeg.arrivalAirport);
    if (!departureEdited && connectionDeparture) setDeparture(connectionDeparture);
  }, [connectionDeparture, departureEdited, previousLeg?.arrivalAirport]);
  useEffect(() => {
    if (arrivalEdited || !arrivalAirport) return;
    const departureTimeZone = departureAirport?.timezone ?? (international ? "" : trip.primary_timezone);
    const suggestion = suggestedFlightLocalTime(departure, departureTimeZone, arrivalAirport.timezone, 180);
    if (suggestion) setArrival(suggestion);
  }, [arrivalAirport, arrivalEdited, departure, departureAirport, international, trip.primary_timezone]);
  useEffect(() => { onArrivalChange?.({ arrivalAirport, arrivalLocal: arrival }); }, [arrival, arrivalAirport]);
  return <JourneyLegCard title={heading} subtitle={routeDescription} removeLabel={`Remove flight leg ${index + 1}`} onRemove={removable ? onRemove : undefined}>
    <div className="grid gap-4 sm:grid-cols-2"><label className="form-label">Airline<RequiredMark /><AirlinePicker name={`${prefix}.airline`} /></label><label className="form-label">Flight number<RequiredMark /><input className="form-input uppercase" name={`${prefix}.number`} placeholder="Enter the number printed on the ticket" required /></label></div>
    <div className="mt-4"><AirportPicker name={`${prefix}.departureName`} codeName={`${prefix}.departureCode`} timezoneName={`${prefix}.departureTimezone`} countryName={`${prefix}.departureCountry`} label="From airport" defaultTimezone={trip.primary_timezone} showManualTimezone={international} initialAirport={previousLeg?.arrivalAirport} onAirportChange={setDepartureAirport} onCountryChange={setDepartureCountry} /></div>
    <div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="form-label">Departure (airport local time)<RequiredMark /><input className="form-input" type="datetime-local" name={`${prefix}.departureAt`} min={`${trip.start_date}T00:00`} max={`${trip.end_date}T23:59`} value={departure} onChange={(event) => { setDepartureEdited(true); setDeparture(event.target.value); }} required /></label>{international ? <label className="form-label">If the departure clock repeats<OccurrenceSelect name={`${prefix}.departureOccurrence`} /></label> : <input type="hidden" name={`${prefix}.departureOccurrence`} value="earlier" />}</div>
    <div className="mt-4"><AirportPicker name={`${prefix}.arrivalName`} codeName={`${prefix}.arrivalCode`} timezoneName={`${prefix}.arrivalTimezone`} countryName={`${prefix}.arrivalCountry`} label="To airport" defaultTimezone={trip.primary_timezone} countryFilter={international ? undefined : departureCountry} showManualTimezone={international} onAirportChange={setArrivalAirport} /></div>
    <div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="form-label">Arrival (airport local time)<RequiredMark /><input className="form-input" type="datetime-local" name={`${prefix}.arrivalAt`} min={`${trip.start_date}T00:00`} max={`${trip.end_date}T23:59`} value={arrival} onChange={(event) => { setArrivalEdited(true); setArrival(event.target.value); }} required /></label>{international ? <label className="form-label">If the arrival clock repeats<OccurrenceSelect name={`${prefix}.arrivalOccurrence`} /></label> : <input type="hidden" name={`${prefix}.arrivalOccurrence`} value="earlier" />}</div>
    <details className="mt-4 rounded-xl border border-line/80 p-3"><summary className="cursor-pointer text-xs font-black uppercase tracking-[.1em] text-muted">Boarding, terminal, and gate</summary><div className="mt-3 grid gap-4 sm:grid-cols-3"><label className="form-label">Boarding lead (minutes)<input className="form-input" type="number" min="0" max="360" name={`${prefix}.boardingLead`} value={boardingLead} onChange={(event) => setBoardingLead(event.target.value)} placeholder="Enter minutes before departure" /></label><label className="form-label">Exact boarding time (optional)<input className="form-input" type="datetime-local" name={`${prefix}.boardingAt`} /></label>{international ? <label className="form-label">If the boarding clock repeats<OccurrenceSelect name={`${prefix}.boardingOccurrence`} /></label> : <input type="hidden" name={`${prefix}.boardingOccurrence`} value="earlier" />}<label className="form-label">Departure terminal<input className="form-input" name={`${prefix}.departureTerminal`} placeholder="Enter the terminal shown on the ticket" /></label><label className="form-label">Departure gate<input className="form-input" name={`${prefix}.departureGate`} placeholder="Enter the gate when known" /></label><label className="form-label">Arrival terminal<input className="form-input" name={`${prefix}.arrivalTerminal`} placeholder="Enter the arrival terminal when known" /></label></div>{calculatedBoarding && <p className="mt-3 rounded-xl bg-brand-soft px-3 py-2 text-xs font-bold text-brand">Calculated boarding time: {calculatedBoarding.replace("T", " ")} at the departure airport. An exact time overrides this.</p>}{international && <RepeatedClockHelp />}</details>
    <TravelerAllocationFields prefix={prefix} travelers={travelers} mode="flight" />
  </JourneyLegCard>;
}

const routeLabels: Record<Exclude<JourneyMode, "cab">, { origin: string; destination: string; operator: string; service: string }> = {
  train: { origin: "Boarding station", destination: "Destination station", operator: "Train operator", service: "Train number" },
  bus: { origin: "Boarding point", destination: "Drop-off point", operator: "Bus operator", service: "Service number" },
  ferry: { origin: "Departure terminal or pier", destination: "Arrival terminal or pier", operator: "Ferry operator", service: "Vessel or service number" }
};

export function GroundJourneyLegFields({ index, mode, trip, scope, travelers, reservationState, direct, removable, onRemove }: { index: number; mode: Exclude<JourneyMode, "cab">; trip: Trip; scope: JourneyScope; travelers: Traveler[]; reservationState: ReservationState; direct: boolean; removable: boolean; onRemove: () => void }) {
  const prefix = `journey.${index}`;
  const labels = routeLabels[mode];
  const [departure, setDeparture] = useState(`${trip.start_date}T09:00`);
  const [boardingLead, setBoardingLead] = useState("");
  const [originName, setOriginName] = useState("");
  const [originCode, setOriginCode] = useState("");
  const [destinationName, setDestinationName] = useState("");
  const [destinationCode, setDestinationCode] = useState("");
  const [ferrySeating, setFerrySeating] = useState("unknown");
  const [vehicle, setVehicle] = useState(false);
  const international = scope === "international";
  const calculatedBoarding = boardingLead ? localDateTimeMinusMinutes(departure, Number(boardingLead)) : "";
  const booked = reservationState === "booked";
  const noun = mode === "ferry" ? "sailing" : mode;
  const originLabel = endpointLabel(originCode, originName);
  const destinationLabel = endpointLabel(destinationCode, destinationName);
  const heading = originLabel && destinationLabel ? `${originLabel} → ${destinationLabel} ${noun}` : direct ? `${noun} details` : index ? `Connection ${index + 1}` : `First ${noun}`;
  const routeDescription = originName && destinationName && (originCode || destinationCode) ? `${originName} to ${destinationName}` : undefined;
  return <JourneyLegCard title={heading} subtitle={routeDescription} removeLabel={`Remove ${mode} leg ${index + 1}`} onRemove={removable ? onRemove : undefined}>
    <div className="grid gap-4 sm:grid-cols-2"><div className="form-label"><span>{labels.operator}{booked && <RequiredMark />}</span><JourneyOperatorPicker mode={mode} name={`${prefix}.operator`} required={booked} /></div><label className="form-label">{labels.service} (optional)<input className="form-input" name={`${prefix}.service`} placeholder={booked ? "Enter the number printed on the ticket, if provided" : "Add the service number later if it is not known yet"} /></label></div>
    {mode === "train" && <label className="form-label mt-4">Train name (optional)<input className="form-input" name={`${prefix}.trainName`} placeholder="Enter the train name if it is already known" /></label>}
    <div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="form-label">{labels.origin}<RequiredMark /><input className="form-input" name={`${prefix}.originName`} onChange={(event) => setOriginName(event.target.value)} placeholder={mode === "train" ? "Search by station name or code" : mode === "bus" ? "Enter the pickup point printed on the ticket" : "Search for the departure terminal or pier"} required /></label><label className="form-label">{labels.origin} code (optional)<input className="form-input uppercase" name={`${prefix}.originCode`} onChange={(event) => setOriginCode(event.target.value)} placeholder="Enter the station or terminal code, if used" /></label>{international && <label className="form-label">Departure country code<RequiredMark /><input className="form-input uppercase" maxLength={2} name={`${prefix}.originCountry`} placeholder="Enter the 2-letter country code" required /></label>}{international ? <label className="form-label">Departure time zone<RequiredMark /><TimeZoneAutocomplete name={`${prefix}.originTimezone`} requireSelection required aria-label="Origin time zone" /></label> : <input type="hidden" name={`${prefix}.originTimezone`} value={trip.primary_timezone} />}</div>
    <div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="form-label">Departure (local time)<RequiredMark /><input className="form-input" type="datetime-local" name={`${prefix}.departureAt`} min={`${trip.start_date}T00:00`} max={`${trip.end_date}T23:59`} value={departure} onChange={(event) => setDeparture(event.target.value)} required /></label>{international ? <label className="form-label">If the departure clock repeats<OccurrenceSelect name={`${prefix}.departureOccurrence`} /></label> : <input type="hidden" name={`${prefix}.departureOccurrence`} value="earlier" />}</div>
    <div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="form-label">{labels.destination}<RequiredMark /><input className="form-input" name={`${prefix}.destinationName`} onChange={(event) => setDestinationName(event.target.value)} placeholder={mode === "train" ? "Search by station name or code" : mode === "bus" ? "Enter the drop-off point printed on the ticket" : "Search for the arrival terminal or pier"} required /></label><label className="form-label">{labels.destination} code (optional)<input className="form-input uppercase" name={`${prefix}.destinationCode`} onChange={(event) => setDestinationCode(event.target.value)} placeholder="Enter the station or terminal code, if used" /></label>{international && <label className="form-label">Arrival country code<RequiredMark /><input className="form-input uppercase" maxLength={2} name={`${prefix}.destinationCountry`} placeholder="Enter the 2-letter country code" required /></label>}{international ? <label className="form-label">Arrival time zone<RequiredMark /><TimeZoneAutocomplete name={`${prefix}.destinationTimezone`} requireSelection required aria-label="Destination time zone" /></label> : <input type="hidden" name={`${prefix}.destinationTimezone`} value={trip.primary_timezone} />}</div>
    <div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="form-label">Arrival (local time, optional)<input className="form-input" type="datetime-local" name={`${prefix}.arrivalAt`} min={`${trip.start_date}T00:00`} max={`${trip.end_date}T23:59`} /></label>{international ? <label className="form-label">If the arrival clock repeats<OccurrenceSelect name={`${prefix}.arrivalOccurrence`} /></label> : <input type="hidden" name={`${prefix}.arrivalOccurrence`} value="earlier" />}</div>
    {booked && mode === "train" && <details className="mt-4 rounded-xl border border-line/80 p-3"><summary className="cursor-pointer text-xs font-black uppercase tracking-[.1em] text-muted">Train ticket details</summary><div className="mt-3 grid gap-4 sm:grid-cols-2"><label className="form-label">Booked from station (optional)<input className="form-input" name={`${prefix}.bookedFromName`} placeholder="Only add this if it differs from the boarding station" /></label><label className="form-label">Booked from code (optional)<input className="form-input uppercase" name={`${prefix}.bookedFromCode`} placeholder="Enter the station code printed under Booked From" /></label><label className="form-label">Travel class (optional)<input className="form-input" name={`${prefix}.travelClass`} placeholder="Enter the class printed on the ticket" /></label><label className="form-label">Quota (optional)<input className="form-input" name={`${prefix}.quota`} placeholder="Enter the quota printed on the ticket" /></label><label className="form-label">Booking status (optional)<input className="form-input" name={`${prefix}.bookingStatus`} placeholder="Enter the original booking status" /></label><label className="form-label">Current status (optional)<input className="form-input" name={`${prefix}.currentStatus`} placeholder="Enter the latest confirmed or waitlist status" /></label></div></details>}
    {booked && mode === "bus" && <details className="mt-4 rounded-xl border border-line/80 p-3"><summary className="cursor-pointer text-xs font-black uppercase tracking-[.1em] text-muted">Bus ticket details</summary><div className="mt-3 grid gap-4 sm:grid-cols-2"><label className="form-label">Bus class or layout (optional)<input className="form-input" name={`${prefix}.busClass`} placeholder="Enter the bus class or seating layout shown on the ticket" /></label><label className="form-label">Boarding point details (optional)<textarea className="form-input min-h-20" name={`${prefix}.boardingPointDetails`} placeholder="Add landmark or pickup instructions from the ticket" /></label><label className="form-label">Drop-off point details (optional)<textarea className="form-input min-h-20" name={`${prefix}.dropoffPointDetails`} placeholder="Add the drop-off landmark or instructions" /></label></div></details>}
    {mode === "ferry" && <div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="form-label">Sailing direction<select className="form-input" name={`${prefix}.direction`}><option value="one_way">One-way</option><option value="outbound">Outbound</option><option value="return">Return sailing</option></select></label>{booked && <><label className="form-label">Ticket timing<select className="form-input" name={`${prefix}.ticketTiming`}><option value="fixed">Fixed date and time</option><option value="open_date">Open date</option><option value="open_return">Open return</option></select></label><label className="form-label">Seating<select className="form-input" name={`${prefix}.seating`} value={ferrySeating} onChange={(event) => setFerrySeating(event.target.value)}><option value="unknown">Unknown</option><option value="free">Free seating</option><option value="assigned">Assigned</option></select></label></>}</div>}
    {booked && mode === "ferry" && <details className="mt-4 rounded-xl border border-line/80 p-3"><summary className="cursor-pointer text-xs font-black uppercase tracking-[.1em] text-muted">Ferry ticket and vehicle details</summary><div className="mt-3 grid gap-4 sm:grid-cols-2"><label className="form-label">Operator reference (optional)<input className="form-input" name={`${prefix}.operatorReference`} placeholder="Enter the ferry company's confirmation number" /></label><label className="form-label">Class / accommodation (optional)<input className="form-input" name={`${prefix}.accommodation`} placeholder="Enter the passenger class, cabin, or accommodation" /></label><label className="form-label">Vessel name (optional)<input className="form-input" name={`${prefix}.vesselName`} placeholder="Enter the vessel name when provided" /></label><label className="form-label">Departure gate (optional)<input className="form-input" name={`${prefix}.departureGate`} placeholder="Enter the gate shown on the ticket" /></label><label className="form-label">Baggage allowance (optional)<input className="form-input" name={`${prefix}.baggageAllowance`} placeholder="Enter the allowance shown on the ticket" /></label><label className="flex items-center gap-2 text-sm font-extrabold sm:col-span-2"><input type="checkbox" checked={vehicle} onChange={(event) => setVehicle(event.target.checked)} /> Taking a vehicle</label>{vehicle && <><label className="form-label">Vehicle type<input className="form-input" name={`${prefix}.vehicleType`} placeholder="Enter car, motorcycle, van, or another type" /></label><label className="form-label">Registration<input className="form-input uppercase" name={`${prefix}.vehicleRegistration`} placeholder="Enter the vehicle registration" /></label><label className="form-label">Length (cm)<input className="form-input" type="number" min="1" name={`${prefix}.vehicleLength`} placeholder="Enter only when the ferry requests it" /></label><label className="form-label">Height (cm)<input className="form-input" type="number" min="1" name={`${prefix}.vehicleHeight`} placeholder="Enter only when the ferry requests it" /></label></>}</div></details>}
    <details className="mt-4 rounded-xl border border-line/80 p-3"><summary className="cursor-pointer text-xs font-black uppercase tracking-[.1em] text-muted">Boarding and platform details</summary><div className="mt-3 grid gap-4 sm:grid-cols-2"><label className="form-label">Boarding reminder (minutes)<input className="form-input" type="number" min="0" max="360" name={`${prefix}.boardingLead`} value={boardingLead} onChange={(event) => setBoardingLead(event.target.value)} placeholder="Enter how early to arrive or board" /></label><label className="form-label">Exact boarding time (optional)<input className="form-input" type="datetime-local" name={`${prefix}.boardingAt`} /></label><label className="form-label">Departure platform / bay (optional)<input className="form-input" name={`${prefix}.departurePlatform`} placeholder="Enter the platform, bay, or gate" /></label><label className="form-label">Arrival platform / bay (optional)<input className="form-input" name={`${prefix}.arrivalPlatform`} placeholder="Enter the arrival platform or bay" /></label></div>{calculatedBoarding && <p className="mt-3 rounded-xl bg-brand-soft px-3 py-2 text-xs font-bold text-brand">Calculated boarding time: {calculatedBoarding.replace("T", " ")} at the departure point.</p>}</details>
    {booked && <TravelerAllocationFields prefix={prefix} travelers={travelers} mode={mode} showSeatAndCabin={mode !== "ferry" || ferrySeating === "assigned"} />}
    {international && <RepeatedClockHelp />}
  </JourneyLegCard>;
}

export function AddConnectionButton({ mode, onClick }: { mode: "flight" | Exclude<JourneyMode, "cab">; onClick: () => void }) {
  return <button type="button" className="secondary-button w-full" onClick={onClick}><Plus className="size-4" /> Add connecting {mode === "ferry" ? "sailing" : mode}</button>;
}

export function CabFields({ trip, reservationState, itineraryTiming, flightLegs }: { trip: Trip; reservationState: ReservationState; itineraryTiming: ReactNode; flightLegs: FlightLeg[] }) {
  const [rideType, setRideType] = useState<"local" | "airport_transfer" | "outstation" | "hourly">("local");
  const [crossBorder, setCrossBorder] = useState(false);
  const [roundTrip, setRoundTrip] = useState(false);
  const booked = reservationState === "booked";
  const completed = reservationState === "walk_up";
  return <>
    <fieldset className="rounded-2xl border border-line p-4"><legend className="px-1 text-sm font-extrabold">Ride type</legend><div className="mt-1 grid gap-2 sm:grid-cols-2">{([['local','Local ride'],['airport_transfer','Airport transfer'],['outstation','Long-distance / outstation'],['hourly','Hire by hour or day']] as const).map(([value,label]) => <label key={value} className={`flex items-center gap-2 rounded-xl border p-3 text-sm font-bold ${rideType === value ? "border-brand bg-brand-soft" : "border-line bg-elevated"}`}><input type="radio" name="cabRideType" value={value} checked={rideType === value} onChange={() => setRideType(value)} />{label}</label>)}</div></fieldset>
    <fieldset className="rounded-2xl border border-line p-4"><legend className="px-1 text-sm font-extrabold">Route</legend><div className="mt-2 grid gap-4 sm:grid-cols-2"><label className="form-label">Pickup<RequiredMark /><input className="form-input" name="cab.pickup" placeholder="Enter where the cab should collect you" required /></label><label className="form-label">Drop-off {rideType === "hourly" ? "(optional)" : <RequiredMark />}<input className="form-input" name="cab.dropoff" placeholder={rideType === "hourly" ? "Enter the final drop-off if already known" : "Enter where the ride should end"} required={rideType !== "hourly"} /></label></div></fieldset>
    {itineraryTiming}
    {(booked || completed) && <details open className="rounded-2xl border border-line p-4"><summary className="cursor-pointer text-sm font-extrabold">{completed ? "Completed ride details" : "Booked ride details"}</summary><div className="mt-4 grid gap-4 sm:grid-cols-2"><div className="form-label"><span>Cab company or app {completed ? "(optional)" : <RequiredMark />}</span><JourneyOperatorPicker mode="cab" name="cab.operator" required={booked} /></div><label className="form-label">Vehicle / service class (optional)<input className="form-input" name="cab.vehicleClass" placeholder="Enter the booked vehicle or service class" /></label><label className="form-label sm:col-span-2">Pickup instructions (optional)<textarea className="form-input min-h-20" name="cab.pickupInstructions" placeholder="Add the pickup zone, door, landmark, or meet-and-greet note" /></label></div></details>}
    <details className="rounded-2xl border border-line p-4"><summary className="cursor-pointer text-sm font-extrabold">More ride details</summary><div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="form-label">Pickup Google Maps link (optional)<input className="form-input" type="url" name="mapUrl" placeholder="Paste the pickup place or directions link" /></label><label className="form-label">Driver name (optional)<input className="form-input" name="cab.driverName" placeholder="Add it later when a driver is assigned" /></label><label className="form-label">Driver phone (optional)<input className="form-input" type="tel" name="cab.driverPhone" placeholder="Add it later when a driver is assigned" /></label><label className="form-label">Vehicle registration (optional)<input className="form-input uppercase" name="cab.vehicleRegistration" placeholder="Add it later when the vehicle is assigned" /></label>{rideType === "airport_transfer" && <><label className="form-label">Linked flight (optional)<select className="form-input" name="cab.linkedFlightLegId" defaultValue="" disabled={!flightLegs.length}><option value="">{flightLegs.length ? "Choose a flight in this trip" : "No flights have been added to this trip"}</option>{flightLegs.map((flight) => <option key={flight.id} value={flight.id}>{`${flight.airline_name} ${flight.flight_number} · ${flight.departure_airport_code || flight.departure_airport_name} → ${flight.arrival_airport_code || flight.arrival_airport_name}`}</option>)}</select></label><label className="form-label">Pickup buffer after landing (minutes)<input className="form-input" type="number" min="0" name="cab.pickupBuffer" placeholder="Enter how long after landing pickup is planned" /></label><label className="form-label">Luggage count (optional)<input className="form-input" type="number" min="0" name="cab.luggageCount" placeholder="Enter the number of bags" /></label></>}{rideType === "outstation" && <><label className="flex items-center gap-2 text-sm font-extrabold"><input type="checkbox" checked={roundTrip} onChange={(event) => setRoundTrip(event.target.checked)} /> Round trip</label>{roundTrip && <label className="form-label">Return date and time<input className="form-input" type="datetime-local" min={`${trip.start_date}T00:00`} max={`${trip.end_date}T23:59`} name="cab.returnAt" /></label>}</>}<label className="flex items-center gap-2 text-sm font-extrabold sm:col-span-2"><input type="checkbox" checked={crossBorder} onChange={(event) => setCrossBorder(event.target.checked)} /> Cross-border ride</label>{crossBorder && <><label className="form-label">Pickup country code<RequiredMark /><input className="form-input uppercase" name="cab.originCountry" maxLength={2} placeholder="Enter the 2-letter country code" required /></label><label className="form-label">Drop-off country code<RequiredMark /><input className="form-input uppercase" name="cab.destinationCountry" maxLength={2} placeholder="Enter the 2-letter country code" required /></label><label className="form-label">Pickup time zone<RequiredMark /><TimeZoneAutocomplete name="cab.originTimezone" requireSelection required /></label><label className="form-label">Drop-off time zone<RequiredMark /><TimeZoneAutocomplete name="cab.destinationTimezone" requireSelection required /></label></>}<label className="form-label sm:col-span-2">Notes (optional)<textarea className="form-input min-h-20" name="notes" placeholder="Add instructions you may need for this ride" /></label></div></details>
    <input type="hidden" name="cab.crossBorder" value={crossBorder ? "yes" : "no"} /><input type="hidden" name="cab.tripShape" value={roundTrip ? "round_trip" : "one_way"} /><input type="hidden" name="cabAlreadyHappened" value={completed ? "yes" : "no"} />
  </>;
}
